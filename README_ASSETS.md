# Amoria V5 activity artwork

Runtime artwork under `admin-web/public/activity-art/` is local and mapped by activity key. Unknown or custom types use `default.jpg`. The artwork is visual-only and must not be added to API, DTO, or database contracts. Files under design references are never loaded at runtime.
