*** Begin Patch
*** Add File: .gitignore
+# Node / JS
+node_modules/
+npm-debug.log*
+yarn-error.log*
+pnpm-lock.yaml
+.pnp.*
+.parcel-cache
+.expo/
+dist/
+build/
+.cache/
+.tmp/
+
+# Env & secrets
+.env
+.env.*
+!.env.example
+
+# OS / IDE
+.DS_Store
+Thumbs.db
+.idea/
+.vscode/
+
*** End Patch
*** Begin Patch
*** Add File: .env.example
+# Пример конфига Firebase (реальные ключи сюда не добавляем)
+FIREBASE_API_KEY=
+FIREBASE_AUTH_DOMAIN=
+FIREBASE_PROJECT_ID=
+FIREBASE_STORAGE_BUCKET=
+FIREBASE_MESSAGING_SENDER_ID=
+FIREBASE_APP_ID=
+FIREBASE_MEASUREMENT_ID=
+
*** End Patch
*** Begin Patch
*** Add File: src/config/firebaseConfig.ts
+// Безопасная заглушка для Firebase.
+// Ключи берём из .env (см. .env.example). Без них проект не упадёт, но Firebase не подключится.
+
+export const firebaseConfig = {
+  apiKey: process.env.FIREBASE_API_KEY ?? "",
+  authDomain: process.env.FIREBASE_AUTH_DOMAIN ?? "",
+  projectId: process.env.FIREBASE_PROJECT_ID ?? "",
+  storageBucket: process.env.FIREBASE_STORAGE_BUCKET ?? "",
+  messagingSenderId: process.env.FIREBASE_MESSAGING_SENDER_ID ?? "",
+  appId: process.env.FIREBASE_APP_ID ?? "",
+  measurementId: process.env.FIREBASE_MEASUREMENT_ID ?? "",
+};
+
+if (!firebaseConfig.apiKey) {
+  // eslint-disable-next-line no-console
+  console.warn(
+    "[firebase] Empty config. Add keys to .env (see .env.example) or fill firebaseConfig.ts"
+  );
+}
+
+export default firebaseConfig;
+s
*** End Patch
