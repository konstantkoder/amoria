import React from 'react';
import { View, Text } from 'react-native';
import { theme } from '@/theme/theme';
export default function ChatScreen(){ return <View style={{flex:1,alignItems:'center',justifyContent:'center', backgroundColor: theme.colors.bg}}><Text>Random Chat (скоро)</Text></View> }