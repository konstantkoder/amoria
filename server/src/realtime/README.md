# Realtime

This directory is reserved for the future Amoria WebSocket layer.

The backend foundation intentionally does not implement chats, Together queues,
presence, or live room state yet. Those features should be added after mobile
auth/profile/media traffic has moved behind the stable API entry point.
